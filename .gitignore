# Build artifacts (Rust + Node)
/target
node_modules/
.vite/

# OS junk
.DS_Store
Thumbs.db

# Editor / IDE swap files and project metadata
*.swp
*.swo
*~
*.bak
.vscode/
.idea/
.vs/
.fleet/
.claude/
*.iml

# Operator-local runtime state generated by `marg start`.
# These land in the launch CWD and must not be committed.
marg.toml
policy.toml
*.db
*.db-wal
*.db-shm
*.key
marg-admin.token
audit/
*.log

# Secrets and per-environment overrides
.env
.env.*
!.env.example
.envrc
*.pem
*.crt
secrets/

# Walkthrough harness run output when the operator runs the harness
# from inside the repo (results land under dist/walkthrough/results/).
dist/walkthrough/results/
