fn main() -> anyhow::Result<()> {
    eprintln!("marg-provider-stub: P00 placeholder. The deterministic OpenAI-compatible fake provider lands in P01 per architecture/testing-strategy.md.");
    Ok(())
}
